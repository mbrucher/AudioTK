
FILE(GLOB
  ATK_REVERBERATION_TEST_SRC
  *.cpp
)

FILE(GLOB
  ATK_REVERBERATION_TEST_HEADERS
  *.h*
)

SET(ATK_REVERBERATION_TEST_DEFINITIONS)
SET(ATK_REVERBERATION_TEST_INCLUDE)
SET(ATK_REVERBERATION_TEST_LIBRARIES ATKReverberation ATKTools ATKMock ATKCore ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY})

SET(ATK_REVERBERATION_TEST_NAME ATKReverberation_test)
SET(ATK_REVERBERATION_TEST_FOLDER Reverberation_test)

ATK_add_test(ATK_REVERBERATION_TEST)
