
FILE(GLOB
    ATK_IO_TEST_SRC
    *.cpp
)

FILE(GLOB
    ATK_IO_TEST_HEADERS
    *.h
)

SET(ATK_IO_TEST_DEFINITIONS)
SET(ATK_IO_TEST_INCLUDE)
SET(ATK_IO_TEST_LIBRARIES ATKIO ATKTools ATKMock ATKCore ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY})

if(LIBSNDFILE_FOUND)
  FILE(GLOB_RECURSE
    ATK_IO_LIBSNDFILE_TEST_SRC
  libsndfile/*.cpp
  )

  FILE(GLOB_RECURSE
  ATK_IO_LIBSNDFILE_TEST_HEADERS
    libsndfile/*.h
  )

  LIST(APPEND ATK_IO_TEST_SRC ${ATK_IO_LIBSNDFILE_TEST_SRC})
  LIST(APPEND ATK_IO_TEST_HEADERS ${ATK_IO_LIBSNDFILE_TEST_HEADERS})
endif(LIBSNDFILE_FOUND)

SET(ATK_IO_TEST_NAME ATKIO_test)
SET(ATK_IO_TEST_TESTNAME IO)
SET(ATK_IO_TEST_FOLDER IO_test)

ATK_add_test(ATK_IO_TEST)
