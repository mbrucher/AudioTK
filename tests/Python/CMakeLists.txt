
if(ENABLE_PYTHON)
  find_package(PythonInterp REQUIRED)
  if(${PYTHON_VERSION_MAJOR} STREQUAL "2")
    find_program(NOSETESTS3 NAMES nosetests3)
    if(NOSETESTS3)
      add_test(PyAudioTK nosetests3 -A="Eigen==False")
      if(EIGEN_FOUND)
        add_test(PyAudioTK_Eigen nosetests3 -A="Eigen==True")
      endif(EIGEN_FOUND)
    else(NOSETESTS3)
      add_test(PyAudioTK nosetests -A="Eigen==False")
      if(EIGEN_FOUND)
        add_test(PyAudioTK_Eigen nosetests -A="Eigen==True")
      endif(EIGEN_FOUND)
    endif(NOSETESTS3)
  else(${PYTHON_VERSION_MAJOR} STREQUAL "2")
    add_test(PyAudioTK nosetests -A="Eigen==False")
    if(EIGEN_FOUND)
      add_test(PyAudioTK_Eigen nosetests -A="Eigen==True")
    endif(EIGEN_FOUND)
  endif(${PYTHON_VERSION_MAJOR} STREQUAL "2")
endif(ENABLE_PYTHON)
