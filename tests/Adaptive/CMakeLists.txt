
if(EIGEN_FOUND)

FILE(GLOB
  ATK_ADAPTIVE_TEST_SRC
  *.cpp
)

FILE(GLOB
  ATK_ADAPTIVE_TEST_HEADERS
  *.h*
)

SET(ATK_ADAPTIVE_TEST_DEFINITIONS)
SET(ATK_ADAPTIVE_TEST_INCLUDE ${EIGEN_INCLUDE_DIRS})
SET(ATK_ADAPTIVE_TEST_LIBRARIES ATKAdaptive ATKTools ATKMock ATKCore ATKUtility ${Boost_UNIT_TEST_FRAMEWORK_LIBRARY})

SET(ATK_ADAPTIVE_TEST_NAME ATKAdaptive_test)
SET(ATK_ADAPTIVE_TEST_FOLDER Adaptive_test)

ATK_add_test(ATK_ADAPTIVE_TEST)

endif(EIGEN_FOUND)
