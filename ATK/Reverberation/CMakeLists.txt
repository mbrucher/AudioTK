
FILE(GLOB_RECURSE
  ATK_REVERBERATION_SRC
  *.cpp
)

FILE(GLOB_RECURSE
  ATK_REVERBERATION_HEADERS
  *.h*
)

SET(ATK_REVERBERATION_DEFINITIONS)
SET(ATK_REVERBERATION_INCLUDE)
SET(ATK_REVERBERATION_LIBRARIES ATKCore)

SET(ATK_REVERBERATION_NAME ATKReverberation)
SET(ATK_REVERBERATION_FOLDER Reverberation)

ATK_add_library(ATK_REVERBERATION)
