if(EIGEN_FOUND)

FILE(GLOB_RECURSE
  ATK_ADAPTIVE_SRC
  *.cpp
)

FILE(GLOB_RECURSE
  ATK_ADAPTIVE_HEADERS
  *.h*
)

SET(ATK_ADAPTIVE_DEFINITIONS)
SET(ATK_ADAPTIVE_INCLUDE)
SET(ATK_ADAPTIVE_LIBRARIES ATKCore ATKUtility)

IF(EIGEN_FOUND)
  LIST(APPEND ATK_ADAPTIVE_INCLUDE ${EIGEN_INCLUDE_DIRS})
ENDIF(EIGEN_FOUND)

SET(ATK_ADAPTIVE_NAME ATKAdaptive)
SET(ATK_ADAPTIVE_FOLDER Adaptive)

ATK_add_library(ATK_ADAPTIVE)

endif(EIGEN_FOUND)
