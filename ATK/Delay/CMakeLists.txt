
FILE(GLOB
  ATK_DELAY_SRC
  *.cpp
)

FILE(GLOB
  ATK_DELAY_HEADERS
  *.h*
)

SET(
  ATK_DELAY_SIMD_SRC
  SIMD/FixedDelayLineFilter.cpp
  SIMD/UniversalFixedDelayLineFilter.cpp
)

FILE(GLOB
  ATK_DELAY_SIMD_HEADERS
  SIMD/*.h*
)

SET(ATK_DELAY_DEFINITIONS)
SET(ATK_DELAY_INCLUDE)
SET(ATK_DELAY_LIBRARIES ATKCore)

IF(EIGEN_FOUND)
  LIST(APPEND ATK_DELAY_INCLUDE ${EIGEN_INCLUDE_DIRS})
ENDIF(EIGEN_FOUND)

SET(ATK_DELAY_NAME ATKDelay)
SET(ATK_DELAY_FOLDER Delay)

ATK_add_library(ATK_DELAY)
