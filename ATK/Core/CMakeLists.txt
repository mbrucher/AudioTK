
FILE(GLOB
    ATK_CORE_SRC
    *.cpp
)

SET(ATK_CORE_SIMD_SRC
    SIMD/TypedBaseFilter.cpp
    SIMD/Utilities.cpp
)

FILE(GLOB
    ATK_CORE_HEADERS
    *.h*
)

SET(ATK_CORE_DEFINITIONS)
SET(ATK_CORE_INCLUDE)
SET(ATK_CORE_LIBRARIES)

if(ENABLE_INTERNAL_PROFILING)
  LIST(APPEND ATK_CORE_LIBRARIES ${Boost_SYSTEM_LIBRARY})
endif(ENABLE_INTERNAL_PROFILING)
if(ENABLE_THREADS)
  LIST(APPEND ATK_CORE_LIBRARIES ${TBB_LIBRARY})
endif(ENABLE_THREADS)

SET(ATK_CORE_NAME ATKCore)
SET(ATK_CORE_FOLDER Core)

ATK_add_library(ATK_CORE)
