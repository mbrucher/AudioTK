
FILE(GLOB
    ATK_UTILITY_SRC
    *.cpp
)

FILE(GLOB
    ATK_UTILITY_HEADERS
    *.h*
)

SET(ATK_UTILITY_DEFINITIONS)
SET(ATK_UTILITY_INCLUDE ${FFT_INCLUDES})
SET(ATK_UTILITY_LIBRARIES ${FFT_LIBRARIES})

SET(ATK_UTILITY_NAME ATKUtility)
SET(ATK_UTILITY_FOLDER Utility)

ATK_add_library(ATK_UTILITY)
