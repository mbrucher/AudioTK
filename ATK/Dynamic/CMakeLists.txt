
FILE(GLOB_RECURSE
  ATK_DYNAMIC_SRC
  *.cpp
)

FILE(GLOB_RECURSE
  ATK_DYNAMIC_HEADERS
  *.h*
)

SET(ATK_DYNAMIC_DEFINITIONS)
SET(ATK_DYNAMIC_INCLUDE)
SET(ATK_DYNAMIC_LIBRARIES ATKCore)

SET(ATK_DYNAMIC_NAME ATKDynamic)
SET(ATK_DYNAMIC_FOLDER Dynamic)

ATK_add_library(ATK_DYNAMIC)
